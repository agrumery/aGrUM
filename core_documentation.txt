  - The class BinTreeNode4AVL shouldn't be called gum::avl::BinTreenode ?
  - How should we write documentation: using this for the class instance or
    using third person ?
    - Returns this child.
    - Returns the node's child.
  - Erase or remove ?
  - Add or insert ?
  - All erase methods do not work identically: some raise NotFound exceptions,
    some don't.
    - Worse, in gum::BinSearchTree erase(const val&) will raise a NotFound
      while erase(iterator) will not...

Renaming classes:

gum::BinSearchTree -> gum::BinarySearchTree
gum::BinTreeNode -> gum::BinaryTreeNode
gum::BinTreeNode4AVL -> gum::AVLTreeNode

Renaming arguments -> chose a convetion between my_var and myVar.
